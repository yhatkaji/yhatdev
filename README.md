# yhatdev

This repository contains a simple Breakout (ブロック崩し) game written in Python using Pygame.

## Requirements
- Python 3
- pygame

Install pygame with:
```bash
pip install pygame
```

## How to run
Execute the following command:
```bash
python breakout.py
```

Move the paddle with the left and right arrow keys and break all the bricks!
